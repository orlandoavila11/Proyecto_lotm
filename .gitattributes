# Auto detect text files and perform LF normalization
* text=auto

# Freeze Tier L: binary preservation (-text) to guarantee byte-for-byte SHA-256 integrity across Windows, Linux and macOS
LOTM_SIMULADOR/reborn/data/content/** -text
